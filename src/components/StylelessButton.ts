import styled from '@emotion/styled';

const StylelessButton = styled.button`
  background: none;
  padding: 0;
  border: none;
  margin: 0;
  cursor: pointer;

  &:focus {
    outline: none;
  }
`;

export default StylelessButton;
